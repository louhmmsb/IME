#include<bits/stdc++.h>
using namespace std;

#define si(x) scanf("%d", &x);
#define pi(x) printf("%d\n", x);
#define pb push_back;

typedef long long int ll;
typedef unsigned long long int ull;

int dp[31][241], d, sumTime, mins[31], maxs[31];

int pd(int dat, int sumat){
    if(sumat == sumTime);
    if(dat == d);

    int& at = dp[dat][sumat];
    if(at != -1) return at;

    at =  
}

int main(){
    si(d);si(sumTime);

    for(int i=0; i<d; i++) si(mins[i]), si(max[i]);
}
